#!/bin/bash

set -ex

y
cd "$(dirname "$0")" || exit 1

(cd jekyll && bundle && bundle exec jekyll build --lsi)
rsync -avz --exclude .git --delete ./jekyll/_site/ aurora:/home/kivikakk/www/kivikakk/
rsync -avz --exclude .git --delete ./archive/ aurora:/home/kivikakk/www/kivikakk/archive/
